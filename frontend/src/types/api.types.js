/**
 * api.types.ts
 *
 * TypeScript types for API request/response shapes.
 * Keep these in sync with backend Pydantic schemas.
 */
export {};
// TODO: Define request and response types for each API domain
// Example:
// export interface LoginRequest { email: string; password: string }
// export interface LoginResponse { accessToken: string; refreshToken: string }
