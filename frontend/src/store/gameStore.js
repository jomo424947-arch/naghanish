/**
 * gameStore.ts
 *
 * Active game session state.
 *
 * Usage:
 *   import { useGameStore } from '@store/gameStore'
 */
import { create } from 'zustand';
// ── Store ─────────────────────────────────────────────────────────────────────
export const useGameStore = create()((_set) => ({
// TODO: Implement initial state and actions
}));
