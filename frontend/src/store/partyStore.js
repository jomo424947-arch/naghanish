/**
 * partyStore.ts
 *
 * Party room and multiplayer session state.
 *
 * Usage:
 *   import { usePartyStore } from '@store/partyStore'
 */
import { create } from 'zustand';
// ── Store ─────────────────────────────────────────────────────────────────────
export const usePartyStore = create()((_set) => ({
// TODO: Implement initial state and actions
}));
