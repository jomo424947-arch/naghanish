/**
 * index.ts — Lib barrel export
 *
 * Re-exports configured wrappers around third-party libraries.
 * Never import third-party libraries directly in components —
 * always go through these wrappers so you can swap them out easily.
 */

// TODO: Export configured library instances:
// export { queryClient } from './queryClient'
// export { framerMotionConfig } from './framerMotion'
// export { zodResolver } from './zodResolver'
