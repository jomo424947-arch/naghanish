/**
 * search/store/index.ts
 *
 * Zustand store slice for the Search feature.
 * Import this store only from within the Search feature boundary.
 * For cross-feature state, use the global store in src/store/
 */
export {};
