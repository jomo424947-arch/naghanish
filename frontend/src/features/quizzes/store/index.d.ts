/**
 * quizzes/store/index.ts
 *
 * Zustand store slice for the Quizzes feature.
 * Import this store only from within the Quizzes feature boundary.
 * For cross-feature state, use the global store in src/store/
 */
export {};
