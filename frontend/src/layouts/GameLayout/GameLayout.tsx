/**
 * GameLayout.tsx
 *
 * Layout wrapper for game and party session pages.
 * Provides a distraction-free game environment.
 */

import { Outlet } from 'react-router-dom'

export function GameLayout() {
  // TODO: Implement game layout (full-screen, minimal UI chrome)
  return <Outlet />
}
