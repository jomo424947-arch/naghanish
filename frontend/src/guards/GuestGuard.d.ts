/**
 * GuestGuard.tsx
 *
 * Prevents authenticated users from accessing guest-only pages
 * (e.g., Login, Register). Redirects them to the home page.
 */
export declare function GuestGuard(): import("react").JSX.Element;
